"""{{package_name}} — main entrypoint."""

# This module handles IO, argument parsing, and wiring.
# Core logic should live in core.py and be imported here.

# from {{package_name}}.core import ...


def main() -> None:
    """Main entrypoint for {{package_name}}."""
    # TODO: implement entrypoint — parse args, call core logic, format output
    print("Hello from {{package_name}}!")


if __name__ == "__main__":
    main()
