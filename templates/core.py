"""{{package_name}} — core logic.

This module contains the primary business logic.
It should have no IO side effects (no file reads, no API calls, no printing).
All IO should be handled by the entrypoint (main.py) or dedicated IO modules.
"""


# TODO: implement core logic based on project requirements
